server:
  port: 8085

spring:
  profiles: # 环境配置
    active: dev

  application:
    name: server

  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mall_user?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Hongkong
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver


# 驼峰下划线转换
#mybatis-plus:
#  configuration:
#    map-underscore-to-camel-case: true
#  mapper-locations:
#    classpath:mapper/**/*.xml

logging:
  level:
    root: DEBUG
